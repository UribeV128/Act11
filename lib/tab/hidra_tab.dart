import 'package:flutter/material.dart';

class HidraTab extends StatelessWidget {
  const HidraTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Hidratante tabla')),
    );
  }
}
