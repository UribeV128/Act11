import 'package:flutter/material.dart';

class ProtTab extends StatelessWidget {
  const ProtTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Protector tabla')),
    );
  }
}
