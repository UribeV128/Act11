import 'package:flutter/material.dart';

class ExfoTab extends StatelessWidget {
  const ExfoTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Exfoliante tabla')),
    );
  }
}
